<%= form_for(@document) do |f| %>
  <% if @document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% @document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :n_format %><br>
    <%= f.number_field :n_format %>
  </div>
  <div class="field">
    <%= f.label :n_project %><br>
    <%= f.number_field :n_project %>
  </div>
  <div class="field">
    <%= f.label :n_version %><br>
    <%= f.number_field :n_version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
