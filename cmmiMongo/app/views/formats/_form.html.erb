<%= form_for(@format) do |f| %>
  <% if @format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@format.errors.count, "error") %> prohibited this format from being saved:</h2>

      <ul>
      <% @format.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :t_name %><br>
    <%= f.text_field :t_name %>
  </div>
  <div class="field">
    <%= f.label :t_content %><br>
    <%= f.text_field :t_content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
